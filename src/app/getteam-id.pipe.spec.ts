import { GetteamIdPipe } from './getteam-id.pipe';

describe('GetteamIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetteamIdPipe();
    expect(pipe).toBeTruthy();
  });
});
