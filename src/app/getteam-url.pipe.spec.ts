import { GetteamUrlPipe } from './getteam-url.pipe';

describe('GetteamUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetteamUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
