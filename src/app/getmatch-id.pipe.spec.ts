import { GetmatchIdPipe } from './getmatch-id.pipe';

describe('GetmatchIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetmatchIdPipe();
    expect(pipe).toBeTruthy();
  });
});
